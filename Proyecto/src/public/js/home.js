const boton = document.getElementById('botonLogout')

boton.addEventListener("click", () => {
    // document.cookie = 'authToken=; Domain=localhost; Path=/; Expires=Thu, 01 Jan 1970 00:00:01 GMT';
    // window.location.replace('/logout');
});